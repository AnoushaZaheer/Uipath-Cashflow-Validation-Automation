# Uipath-Cashflow-Validation-Automation
UiPath automation that validates cash flow records from Excel, handles data exceptions, calculates differences between Cash In and Cash Out values, and updates processing status automatically.
